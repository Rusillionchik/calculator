const sum = (a, b) => a + b


const substract = (a, b) => a - b


const multiply = (a, b) => a * b


const division = (a, b) => {
if (b === 0) {
return 'Нельзя делить число на ноль'
}
return a / b
}
